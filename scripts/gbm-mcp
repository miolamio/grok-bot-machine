#!/usr/bin/env bash
# Host MCP stdio entry for grok-bot-machine. Does not drive the Mac.
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONUNBUFFERED=1
exec python3 "$ROOT/mcp/gbm_mcp.py"
